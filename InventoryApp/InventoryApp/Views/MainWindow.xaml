<Window x:Class="InventoryApp.MainWindow"

xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"

xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

xmlns:local="clr-namespace:InventoryApp"

mc:Ignorable="d"

Title="Product Inventory" Height="250" Width="350">

    <Grid Margin="10">

        <Grid.ColumnDefinitions>

            <ColumnDefinition Width="Auto"/>

            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>

            <RowDefinition Height="Auto"/>

            <RowDefinition Height="Auto"/>

            <RowDefinition Height="Auto"/>

            <RowDefinition Height="Auto"/>

        </Grid.RowDefinitions>

        <TextBlock Text="ProductName:" Grid.Row="0" Grid.Column="0" Margin="5" />

        <TextBox Grid.Row="0" Grid.Column="1" Width="150" Height="25" Margin="5" Text="{Binding ProductName, Mode=TwoWay , UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Text="Quantity:" Grid.Row="1" Grid.Column="0" Margin="5"/>

        <TextBox Grid.Row="1" Grid.Column="1" Width="150" Height="25" Margin="5" Text="{Binding Quantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Text="Status:" Grid.Row="2" Grid.Column="0" Margin="5"/>

        <TextBlock Grid.Row="2" Grid.Column="1" Margin="5" Text="{Binding StatusMessage , Mode=TwoWay}"/>

        <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="5">

            <Button Content="Add to Inventory" IsEnabled="{Binding AddInventoryVisibility}" Width="100" Height="25" Margin="5" Command="{Binding AddInventoryCommand, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <Button Content="Reset" Width="100" Height="25" Margin="5" Command="{Binding ResetCommand, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        </StackPanel>

    </Grid>

</Window>