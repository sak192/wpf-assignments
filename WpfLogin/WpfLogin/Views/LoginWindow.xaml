<Window x:Class="WpfLogin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfLogin"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <local:LoginButtonStateToBoolConverter x:Key="LoginButtonStateToBoolConverter"/>
    </Window.Resources>
    <StackPanel>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
            <Label Content="Username : " HorizontalAlignment="Right" FontSize="20" Grid.Row="0" Grid.Column="0"></Label>
            <Label Content="Password : " HorizontalAlignment="Right" FontSize="20" Grid.Row="1" Grid.Column="0"></Label>
            <TextBox Height="30" Width="250" Grid.Row="0" Grid.Column="1" Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <PasswordBox x:Name="PasswordBox" Height="30" Width="250" Grid.Row="1" Grid.Column="1" ></PasswordBox>
        </Grid>
        <Button IsEnabled="{Binding LoginButtonState, Converter={StaticResource LoginButtonStateToBoolConverter}}" Content="Login" Height="50" Width="100" HorizontalAlignment="Center" Command="{Binding LoginCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"></Button>
    </StackPanel>
</Window>
